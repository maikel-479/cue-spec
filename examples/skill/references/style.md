# Style rules

Loaded only when a thorough review touches formatted code.

- Match surrounding style; do not reformat unrelated lines.
- No trailing whitespace; LF line endings.
- Names: `snake_case` for variables/functions, `PascalCase` for types.
